import { Profile } from "../../components/Profile/Profile";
import { Posts } from "../../components/Posts/Posts";

export const Main = () => {
  return (
    <section>
      <Profile />
      <Posts/>
    </section>
  );
};

